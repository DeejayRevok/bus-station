name: Bus station CI

on: [push]

jobs:
  bus-station-CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup environment
        run: docker-compose up -d
      - name: Check imports
        run: docker exec bus-station poetry run tox -e isort -- --check
      - name: Check formatting
        run: docker exec bus-station poetry run tox -e black -- --check
      - name: Check style
        run: docker exec bus-station poetry run tox -e flake8
      - name: Check typing
        run: docker exec bus-station poetry run tox -e pyre
      - name: Run unit tests
        run: docker exec bus-station poetry run tox -e unit_tests
      - name: Run integration tests
        run: docker exec bus-station poetry run tox -e integration_tests
      - name: Check tests coverage
        run: docker exec bus-station poetry run tox -e check_tests_coverage
      - name: Build the package
        run: docker exec bus-station poetry build
